
  # Optimize Project Requirements

  This is a code bundle for Optimize Project Requirements. The original project is available at https://www.figma.com/design/EbxY7bY1xahCjCkg78Dkvy/Optimize-Project-Requirements.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  